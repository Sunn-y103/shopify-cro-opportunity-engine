{
  "name": "shopify-cro-engine",
  "version": "1.0.0",
  "description": "Shopify CRO Engine - Single Render Deployment",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm install --prefix backend --omit=dev && node --max-old-space-size=400 backend/src/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  },
  "engines": {
    "node": "22.x"
  }
}